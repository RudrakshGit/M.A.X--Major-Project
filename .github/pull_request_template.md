## Outcome

Which brief, and what is true now that was not before.

## Affected tracks / domains

## Validation

- [ ] `npm run check` — paste the real exit code:
- [ ] Playwright screenshot attached, if UI changed
- [ ] Both `en` and `hi` strings present, if user-facing text changed

## Safety

- [ ] No model call added outside `src/safety/`
- [ ] Golden test set still passes, if `src/safety/` was touched
- [ ] No personal data added to a prompt or a log

## Risk and rollback

## Follow-ups
